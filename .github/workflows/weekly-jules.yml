name: Weekly Task for Jules
on:
  schedule:
    - cron: '0 0 * * 1'
  workflow_dispatch:
jobs:
  task:
    runs-on: ubuntu-latest
    steps:
      - name: Notify
        run: echo "Starting weekly Digital Factory discovery..."
